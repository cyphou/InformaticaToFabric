# Auto-generated by Sprint 93 — Kubernetes Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: informatica-migration
  labels:
    app: informatica-migration
    component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: informatica-migration
      component: api
  template:
    metadata:
      labels:
        app: informatica-migration
        component: api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: migration-api
          image: informatica-migration:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: INFORMATICA_MIGRATION_TARGET
              valueFrom:
                configMapKeyRef:
                  name: migration-config
                  key: target
          envFrom:
            - secretRef:
                name: migration-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          volumeMounts:
            - name: config-volume
              mountPath: /app/migration.yaml
              subPath: migration.yaml
              readOnly: true
            - name: output-volume
              mountPath: /app/output
      volumes:
        - name: config-volume
          configMap:
            name: migration-config
        - name: output-volume
          persistentVolumeClaim:
            claimName: migration-output-pvc
